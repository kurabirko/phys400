name: release
on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        documents:
          - proposal

    steps:
      - uses: actions/checkout@v2

      - name: Build documents using LaTeX (${{ matrix.documents }})
        uses: xu-cheng/latex-action@v2
        with:
          working_directory: src
          latexmk_use_lualatex: true
          root_file: ${{ matrix.documents }}/main.tex
          extra_fonts: |
            ../fonts/*.ttf

      - name: Rename PDF output (${{ matrix.documents }})
        run: mv src/main.pdf src/${{ matrix.documents }}.pdf 

      - name: Upload build assets (${{ matrix.documents }})
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.documents }}
          path: src/${{ matrix.documents }}.pdf

  pre-release:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v2
      
      - uses: actions/download-artifact@v2
        with:
          path: ./artifacts

      - uses: marvinpinto/action-automatic-releases@latest
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          automatic_release_tag: latest
          prerelease: true
          title: Latest Build
          files: ./artifacts/*/*.pdf
